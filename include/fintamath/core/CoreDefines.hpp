#pragma once

#include <functional>
#include <map>
#include <memory>
#include <string>
#include <vector>

namespace fintamath {

using std::string;

using std::vector;

using std::map;

using std::multimap;

using std::function;

using std::unique_ptr;

using std::shared_ptr;

using std::make_unique;

using std::make_shared;

using std::move;

using std::forward;

}
